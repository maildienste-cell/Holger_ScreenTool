{
  "name": "desktop-mini-agent",
  "version": "1.0.1",
  "description": "Desktop Mini Agent (Antigravity)",
  "author": "Holger",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . --overwrite --platform=darwin --arch=x64 --icon=robotTemplate.png --prune=true --out=release-builds"
  },
  "devDependencies": {
    "electron": "^31.0.0",
    "electron-packager": "^17.1.2"
  },
  "dependencies": {
    "dompurify": "^3.4.1",
    "marked": "^18.0.2",
    "pdf-parse": "^1.1.1",
    "node-llama-cpp": "^3.1.1"
  }
}
